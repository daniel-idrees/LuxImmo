---
name: adding-a-feature
description: Start-to-finish checklist for adding a brand-new feature end to end — module setup, domain/data wiring, MVI + Compose UI, navigation, UI models, and tests. Reference when adding a new feature.
version: "1.0"
---

# Adding a New Feature

This skill is a step-by-step recipe for adding a brand-new piece of functionality to the app from start to finish. Following the same recipe every time means each feature is built consistently and nothing important is forgotten or left half-done.

## Technical details

The full start-to-finish checklist lives in [`references/details.md`](references/details.md). A generic worked example (the "Articles" feature file-by-file) is in [`examples/`](examples/).
