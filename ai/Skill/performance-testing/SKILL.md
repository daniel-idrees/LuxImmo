---
name: performance-testing
description: Android performance testing — a Macrobenchmark module (com.android.test) that measures startup and generates Baseline Profiles. Reference when writing performance tests or setting up benchmarking and baseline profiles.
version: "1.0"
---

# Measuring Performance

This skill is about measuring how fast the app is — especially how quickly it starts up — and producing data that helps the app run faster and smoother for real users.

## Technical details

The tooling used to measure startup and generate performance data lives in [`references/details.md`](references/details.md). Generic worked examples (a startup benchmark and a baseline-profile generator) are in [`examples/`](examples/).
